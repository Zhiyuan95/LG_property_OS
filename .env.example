# Property listings remain mock until a licensed listing provider is connected.
PROPERTY_DATA_PROVIDER=mock
# Configure in local Settings or copy this file to .env.local.
# Never use NEXT_PUBLIC_ for secrets. Never commit .env.local.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
